# Build artifacts
/build/

# Local ghost store (created by --local mode)
ghosts.json

# Mesh identity stores (created by mesh deploy)
mesh-identities.json
okta-ghosts.json

# Pastebin-style leak simulator output
/pastebin-sim.html

# Saved attacker journey files
attack-journey-*.json

# Terraform
/terraform/.terraform/
/terraform/*.tfstate
/terraform/*.tfstate.*
/terraform/terraform.lock.hcl
